# Athlete OS API
API_BASE_URL=http://localhost:3000/api/v1
API_KEY=dev-local-key

# HTTP server (messaging service calls this for real-time responses)
COACHING_ENGINE_PORT=3002

# Anthropic
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Engine settings (overrides user_settings.json defaults)
# LOG_LEVEL=info
# CONTEXT_MODE=balanced
# ENGINE_MODE=structured

# Job schedules (cron syntax, overrides user_settings.json)
# WEEKLY_PLANNER_CRON=0 20 * * 0
# SNAPSHOT_WRITER_CRON=30 20 * * 0
# PROGRESSION_CHECKER_CRON=0 21 * * 0
# DAILY_DIGEST_CRON=0 9 * * *
